name: Setup Node.js with up-to-date npm
description: |
  Sets up Node.js and installs npm@11 into a fresh prefix on PATH so
  `min-release-age` (npm 11.5+) is honored. Installing into a separate
  prefix avoids the self-overwrite that breaks `npm i -g npm@11` on
  Node 22's bundled npm 10. Keep this on the npm 11 line to match
  engines.npm and .tool-versions.

inputs:
  node-version:
    description: Node.js version spec (e.g. "24.x")
    required: false
    default: "24.x"
  registry-url:
    description: npm registry URL to authenticate against
    required: false
    default: "https://npm.pkg.github.com"

runs:
  using: composite
  steps:
    - name: Use Node.js
      uses: actions/setup-node@v6
      with:
        node-version: ${{ inputs.node-version }}
        registry-url: ${{ inputs.registry-url }}

    - name: Install npm
      shell: bash
      run: |
        npm install --prefix "$RUNNER_TEMP/npm-cli" npm@11
        echo "$RUNNER_TEMP/npm-cli/node_modules/.bin" >> "$GITHUB_PATH"
